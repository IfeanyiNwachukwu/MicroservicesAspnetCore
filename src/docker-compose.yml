version: '3.4'

services:
  catalogueDb:
    image: mongo
  catalogue.api:
    image: ${DOCKER_REGISTRY-}catalogueapi
    build:
      context: .
      dockerfile: Services/Catalogue/Catalogue.API/Dockerfile

volumes:
  mongo_data:
